describe('staff.controller:', function () {

    var directive, testHelpers;
    //
    // beforeEach(module('ssNg'));
    //
    // //beforeEach(module('Olive.testHelpers'));
    //
    // beforeEach(inject(function (_$window_, _$rootScope_, _$compile_, _testHelpers_) {
    //
    // }));

    it('Should get the type of a JavaScript object', function () {
        expect('dog').toEqual('dog');
    });
});